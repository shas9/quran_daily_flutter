name: quran_daily
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter
  flutter_bloc: 8.1.4
  equatable: 2.0.5
  dartz: 0.10.1
  dio: 5.4.0
  logger: 2.4.0
  shared_preferences: 2.0.15

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
